#include "Daat.hpp"

using namespace std;

bool isAndMergeFinished();
bool isOrMergeFinished();

vector<int> Daat::andMerge(vector< vector<int> >& vec_list) {
  vector<int> andResult;
	int listPtr[vec_list.size()] = {}; //全部0で初期化

	while (!isAndMergeFinished()) {
	}
}

vector<int> Daat::orMerge(vector< vector<int> >& vec_list) {
}

bool isAndMergeFinished() {
}

bool isOrMergeFinished() {
}
